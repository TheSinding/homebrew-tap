# TheSinding Homebrew Tap

Install `teamsctl`:

```sh
brew install TheSinding/tap/teamsctl
```

Upgrade it later with:

```sh
brew upgrade teamsctl
```

See [TheSinding/teamsctl](https://github.com/TheSinding/teamsctl) for authentication and usage documentation.
